{
  "name": "lmtagscript-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @lmtagscript/api dev",
    "test": "pnpm -r test",
    "lint": "eslint . || echo \"no eslint config yet\"",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "eslint": "^9.7.0",
    "typescript": "^5.6.2",
    "tsx": "^4.16.0"
  }
}
